apiVersion: v1
kind: Service
metadata:
  name: goserver
spec:
  selector:
    app: goserver
  type: ClusterIP
  ports:
    - name: server-porta
      port: 8000
      targetPort: 8000
      protocol: TCP